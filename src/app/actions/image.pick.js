// import { openPicker } from 'app/actions/image.pick';


export default function imagePick(obj) {
    // const {env, options} = obj;
    // const PICKER_TYPE = options.via === 'camera' ? PIC_FROM_CAMERA : PIC_FROM_GALLERY;
    // store.dispatch(openPicker(PICKER_TYPE, env, options));
}
