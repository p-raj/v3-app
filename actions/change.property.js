export default function changeProperty(obj) {
    // Do nothing here. Doing nothing will resolve the promise leading to success action being triggered
    // FIXME: find a way to correct this and trigger success or error chained action correctly
}